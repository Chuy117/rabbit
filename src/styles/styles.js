import { makeStyles } from "@material-ui/core/styles";

const useStyle = makeStyles({

    fabButton: {
        color: '#FFF',
        background: '#00aef2',
        position: 'absolute',
        bottom: '1%',
        right: '1%'
    },

})

export {

    useStyle

}